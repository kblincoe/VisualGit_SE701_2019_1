export interface ITheme {
  navbarClass: string;
  iconColor: string;
  filePanelColor: string;
  paragraphColor: string;
  headingColor: string;
  diffPanelColor: string;
  diffPanelBackgroundColor: string;
  networkColor: string;
  footerColor: string;
  repositoryPanelColor: string;
  authenticateColor: string;
  navbarToggleColor: string;
}
